import 'package:flutter/material.dart';

import '../hours/components/body.dart';

class SubmitHoursScreen extends StatelessWidget {
  const SubmitHoursScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
